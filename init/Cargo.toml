[package]
name = "muak-init"
version = "0.1.0"
edition = "2021"

[dependencies]
nix = { version = "0.29", features = ["mount", "fs", "dir", "ioctl"] }

[profile.release]
opt-level = "z"
lto = true
strip = true
panic = "abort"
codegen-units = 1
