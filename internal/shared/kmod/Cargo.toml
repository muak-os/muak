[package]
name = "kmod"
version = "0.1.0"
edition = "2024"

[dependencies]
nix = { workspace = true, features = ["fs"] }
zstd = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
