[package]
name = "yuki"
version = "0.1.0"
edition = "2024"
authors = ["Sawang"]
description = "UKI builder - add PE sections to EFI stub"

[dependencies]
# CLI parsing
clap = { workspace = true }

# Utilities
object = { workspace = true }

# Error handling
anyhow = { workspace = true }

[lints]
workspace = true
