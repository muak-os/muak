[package]
name = "yuki"
version = "0.1.0"
edition = "2024"
authors = ["Sawang"]
description = "Static UKI builder - adds PE sections to EFI stubs"

[dependencies]
object = { workspace = true }
clap = { workspace = true }
