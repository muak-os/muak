[package]
name = "muak-stub"
version = "0.1.0"
edition = "2024"

[dependencies]
uefi = { version = "0.36", features = ["alloc", "logger"] }

[profile.dev]
panic = "abort"

[profile.release]
opt-level = "z"
lto = true
strip = true
panic = "abort"
codegen-units = 1
