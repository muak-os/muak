[package]
name = "init"
edition.workspace = true
version.workspace = true

[dependencies]
# Error handling
anyhow = { workspace = true }

# Internal
kmod = { workspace = true }
kmsg = { workspace = true }

# System/platform APIs
rustix = { workspace = true, features = ["process", "mount", "fs"] }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
