[package]
name = "init"
version = "0.1.0"
edition = "2024"

[dependencies]
# System/platform APIs
nix = { workspace = true, features = ["dir", "ioctl", "feature"] }

# Error handling
anyhow = { workspace = true }

# Internal
kmod = { path = "../shared/kmod" }
kmsg = { path = "../shared/kmsg" }

[lints]
workspace = true
