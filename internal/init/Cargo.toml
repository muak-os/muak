[package]
name = "init"
version = "0.1.0"
edition = "2024"

[dependencies]
nix = { workspace = true, features = ["dir", "ioctl"] }
kmod = { path = "../shared/kmod" }
kmsg = { path = "../shared/kmsg" }

[lints]
workspace = true
