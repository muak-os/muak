[package]
name = "imager"
version = "0.2.0"
edition = "2024"
authors = ["Sawang"]
description = "CLI tool to manage OCI images and initramfs generation"

[dependencies]
# CLI parsing
clap = { workspace = true }

# OCI layer extraction
tar = { workspace = true }
flate2 = { workspace = true }

# HTTP client
reqwest = { workspace = true, features = ["blocking", "json"] }

# Serialization
serde = { workspace = true }
serde_json = { workspace = true }

# Filesystem utilities
backhand = { workspace = true }
cpio = { workspace = true }
tempfile = { workspace = true }
walkdir = { workspace = true }
zstd = { workspace = true }

# Error handling
anyhow = { workspace = true }

[lints]
workspace = true
